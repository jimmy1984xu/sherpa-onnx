# AGENTS

- Always use UTF-8 encoding for reading, writing, and editing files.
- Before making changes, clarify the proposed approach with the user and get confirmation, unless the change is trivially simple.
- Do not create Git commits unless the user explicitly requests one.
